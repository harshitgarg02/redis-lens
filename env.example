# RedisLens Environment Configuration
# Copy this file to .env and modify the values for your environment

# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here-generate-a-new-one
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Logging Configuration
# Controls the verbosity of logs in console and log files
# Valid levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO (or DEBUG if DJANGO_DEBUG=True and this is not set)
# DJANGO_LOG_LEVEL=INFO

# Logging Level Examples:
# Development (verbose): DJANGO_LOG_LEVEL=DEBUG
# Production (minimal): DJANGO_LOG_LEVEL=WARNING
# Silent mode: DJANGO_LOG_LEVEL=ERROR

# Database Configuration
# Use 'sqlite' for simple development or 'postgresql' for production-like development
DATABASE_ENGINE=sqlite

# SQLite settings (default - no additional configuration needed)
# Database file will be created as db.sqlite3

# PostgreSQL settings (for development or production)
# Uncomment these if using DATABASE_ENGINE=postgresql
# DB_NAME=redislens_dev
# DB_USER=postgres  
# DB_PASSWORD=your-secure-password
# DB_HOST=localhost
# DB_PORT=5432

# Production PostgreSQL example:
# DB_NAME=redislens_production
# DB_USER=redislens_user
# DB_PASSWORD=very-secure-production-password
# DB_HOST=db.yourcompany.com
# DB_PORT=5432

# OAuth Configuration (Optional - for SSO integration)
# Leave these blank if you want to use local authentication only

# Required OAuth Settings:
# OAUTH_AUTHN_URL=your-oauth-provider-authorization-url
# OAUTH_CLIENT_ID=your-oauth-client-id
# OAUTH_CLIENT_SECRET=your-oauth-client-secret

# Optional OAuth Settings:
# OAUTH_TOKEN_URL=your-oauth-token-url  # Auto-detected if not set
# OAUTH_REDIRECT_URI=/oauth/callback/  # Auto-detects current host if path-only, or use full URL
# OAUTH_SCOPE=openid profile email

# For Development (localhost) - Minimal Configuration:
# OAUTH_AUTHN_URL=https://accounts.google.com/o/oauth2/v2/auth
# OAUTH_CLIENT_ID=your-google-client-id
# OAUTH_CLIENT_SECRET=your-google-client-secret
# OAUTH_REDIRECT_URI=/oauth/callback/
# Note: Will automatically become http://localhost:8000/oauth/callback/

# For Production - Auto Host Detection:
# OAUTH_AUTHN_URL=https://your-company-sso.com/oauth/authorize
# OAUTH_CLIENT_ID=redislens-production
# OAUTH_CLIENT_SECRET=your-production-client-secret
# OAUTH_REDIRECT_URI=/oauth/callback/
# Note: Will automatically become https://your-domain.com/oauth/callback/

# Manual Override (if needed):
# OAUTH_REDIRECT_URI=https://custom-domain.com/oauth/callback/

# Common OAuth Providers:
# Google:
#   OAUTH_AUTHN_URL=https://accounts.google.com/o/oauth2/v2/auth
#   OAUTH_TOKEN_URL=https://oauth2.googleapis.com/token
# Azure AD:
#   OAUTH_AUTHN_URL=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/authorize
#   OAUTH_TOKEN_URL=https://login.microsoftonline.com/{tenant-id}/oauth2/v2.0/token
# Okta:
#   OAUTH_AUTHN_URL=https://{your-domain}.okta.com/oauth2/default/v1/authorize
#   OAUTH_TOKEN_URL=https://{your-domain}.okta.com/oauth2/default/v1/token
# Generic OAuth 2.0:
#   OAUTH_AUTHN_URL=https://your-oauth-provider.com/oauth/authorize
#   OAUTH_TOKEN_URL=https://your-oauth-provider.com/oauth/token

# Application Settings
# Add any additional settings here as needed
